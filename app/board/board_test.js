'use strict';

describe('cookieJar.board module', function() {

  beforeEach(module('cookieJar.board'));

  describe('board controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var boardCtrl = $controller('BoardCtrl');
      expect(boardCtrl).toBeDefined();
    }));

  });
});