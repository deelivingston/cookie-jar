'use strict';

describe('cookieJar.jarapp module', function() {

  beforeEach(module('cookieJar.jarapp'));

  describe('jarapp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jarAppCtrl = $controller('JarAppCtrol');
      expect(jarAppCtrl).toBeDefined();
    }));

  });
});